# Package signing policy is intentionally explicit, but signing is delegated
# to the target Arch environment and is not required for the first ISO build.
OMA_PACMAN_SIGLEVEL="Required DatabaseOptional"
OMA_SIGNING_KEY=""

