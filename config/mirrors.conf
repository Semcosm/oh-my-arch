# Override this value for a local Arch mirror. The URL must expose /core and
# /extra repositories and may contain the pacman $arch placeholder.
OMA_MIRROR_SERVER="https://mirrors.sjtug.sjtu.edu.cn/archlinux"
