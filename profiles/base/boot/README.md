# Shared boot extensions

Bootloader-specific files are copied to the top-level archiso profile by the
oma backend. The first release provides small BIOS syslinux and UEFI
systemd-boot configurations for the generic x86_64 target.
